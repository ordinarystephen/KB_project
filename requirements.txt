# Runtime dependencies for environments that install with pip rather than uv.
# Activate the target virtual environment, then run:
#   python -m pip install -r requirements.txt

azure-ai-documentintelligence>=1.0.2
azure-identity>=1.19.0
jsonschema>=4.23.0
langchain-openai>=0.3.0
pypdf>=5.0.0
python-docx>=1.1.2
pyyaml>=6.0
streamlit>=1.40.0
